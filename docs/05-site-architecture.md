# ۵ — نقشه سایت و معماری اطلاعات

> ساختار کلی سایت: صفحات، ناوبری، و مسیر حرکت کاربر.

---

## ۱. درخت صفحات

```mermaid
flowchart TD
    Home[🏠 خانه] --> About[درباره ما]
    Home --> Courses[دوره‌ها]
    Home --> Services[خدمات]
    Home --> Blog[وبلاگ]
    Home --> Contact[تماس با ما]
    Courses --> Course1[صفحه دوره ۱]
    Courses --> Course2[صفحه دوره ۲]
    Courses --> Course3[صفحه دوره ۳]
    Courses --> CourseN[...]
    Blog --> Post1[مقاله ۱]
    Blog --> Post2[مقاله ۲]
    Home --> Landing[لندینگ‌ها]
    Landing --> L1[لندینگ دوره ۱]
    Home --> Legal[صفحات قانونی]
    Legal --> Terms[قوانین]
    Legal --> Privacy[حریم خصوصی]
    Legal --> FAQ[سوالات متداول]
```

---

## ۲. ناوبری اصلی (Header)

| آیتم منو | لینک به | ترتیب |
|----------|---------|-------|
| خانه | `/` | ۱ |
| دوره‌ها | `/courses` | ۲ |
| خدمات | `/services` | ۳ |
| وبلاگ | `/blog` | ۴ |
| درباره ما | `/about` | ۵ |
| تماس با ما | `/contact` | ۶ |

### دکمه‌ی CTA در هدر
- متن: ⏳ TODO — (مثلاً «مشاهده دوره‌ها»)
- لینک: `/courses`

---

## ۳. فوتر (Footer)

- خلاصه‌ی برند + توضیح کوتاه
- لینک‌های سریع (دوره‌ها، درباره، وبلاگ، تماس)
- لینک صفحات قانونی (قوانین، حریم خصوصی)
- شبکه‌های اجتماعی ⏳ TODO
- اطلاعات تماس ⏳ TODO

---

## ۴. مسیر کاربر (User Journey / Funnel)

```mermaid
flowchart LR
    A[ورود از گوگل/تبلیغ] --> B[صفحه فرود یا خانه]
    B --> C[مشاهده دوره]
    C --> D{اعتماد دارد؟}
    D -- بله --> E[ثبت‌نام / خرید]
    D -- خیر --> F[مطالعه مقاله یا FAQ]
    F --> C
    E --> G[ورود به دوره]
```

---

## ۵. ساختار URL و سئو

| نوع | الگوی URL |
|-----|-----------|
| صفحه اصلی | `/` |
| دوره (تکی) | `/courses/{slug}` |
| مقاله | `/blog/{slug}` |
| لندینگ | `/{slug}-landing` یا `/lp/{slug}` |
| قانونی | `/terms`, `/privacy`, `/faq` |

---

## ۶. یادداشت‌های معماری

- ⏳ TODO — تصمیم درباره‌ی سیستم فروش دوره: LMS (مثل LearnDash) یا ووکامرس یا لندینگ + فرم.
- ⏳ TODO — تصمیم درباره‌ی صفحه‌ی حساب کاربری و ورود/ثبت‌نام.
